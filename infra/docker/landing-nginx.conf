server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Page unique, pas de routing cote client a gerer (contrairement au
  # SPA React de apps/web) : un simple fallback suffit.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
